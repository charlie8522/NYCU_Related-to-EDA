This code can run on local but seems may have some error on session because of gcc version difference.
If you want to run this code,open code::block and click Project→set program's argument→fill in inputs in "Program arguments:"
Then,click "build and run" that will generate the output.  